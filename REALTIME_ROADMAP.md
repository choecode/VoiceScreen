# VoiceScreen 二期：低延迟与实时语音翻译方案

## 1. 一期基线

一期已经形成可交付基线：只捕获 Discord、本地英文识别与英译中、中文按键翻译并发送英文语音、回声隔离、字幕历史、悬浮窗移动缩放均可用。当前策略是等待约 2 秒静音后，把完整语音段交给 Whisper，因此优先保证完整性和稳定性，不追求边说边出字。

开发机硬件：

- Windows 11
- Intel Core i7-14700KF，20 核 28 线程
- NVIDIA GeForce RTX 4070 Ti，12GB 显存
- 游戏时应优先保护显卡帧率和显存，不建议常驻 Whisper large-v3 与大型翻译 LLM。

## 2. 二期目标怎么定义

“实时”不能只看最终一句完成时间，应拆成四个指标：

| 指标 | 推荐目标 | 用户看到的效果 |
|---|---:|---|
| 首个英文临时字幕 | 0.5–1.2 秒 | 对方还在说时已经开始出字 |
| 英文稳定前缀 | 1.0–2.0 秒 | 已稳定的词不再频繁变化 |
| 首段中文译文 | 1.5–3.0 秒 | 中文按短语逐步出现 |
| 停顿后的最终定稿 | 0.5–1.2 秒 | 修正当前行，然后写入历史 |

二期验收同时关注：P50/P95 延迟、句首和句尾丢失率、字幕回改次数、CPU/GPU/显存占用，以及游戏帧时间变化。仅报告“平均速度”不够。

## 3. 推荐总体架构

```text
Discord 40ms PCM
  → 环形缓冲区
  → 流式 VAD / 语音活动状态
  → 增量 ASR（每 320–640ms 更新）
  → 稳定前缀判定（连续两次结果一致）
  → 增量英译中（只翻译新增的稳定短语）
  → 当前字幕行原地更新
  → 停顿后定稿并写入 200 条历史
```

界面必须区分两种内容：

- **临时结果**：颜色略淡，可以被下一次识别修正，只占一条“当前行”。
- **已确认结果**：颜色正常，不再修改，进入历史列表。

不能把每次临时识别都追加为新字幕，否则会出现大量重复句子；也不能每 300ms 翻译整段全文，否则会浪费算力并导致中文持续跳动。

## 4. 三档运行策略

### 4.1 稳定档：一期兼容模式

- faster-whisper small，CPU INT8。
- OPUS-MT，CPU INT8。
- 完整语音段结束后识别和翻译。
- 优点：不抢显卡，结果最稳定，现有版本可随时回退。
- 缺点：不是边说边显示。

### 4.2 推荐档：游戏低延迟模式

- faster-whisper small 多语言模型放到 RTX 4070 Ti，使用 CUDA `int8_float16`。
- OPUS-MT 继续运行在 CPU，避免 ASR、翻译和游戏同时争夺显卡。
- 每 320–640ms 输入新音频，保留约 8–12 秒滚动上下文。
- 使用 LocalAgreement-2：相邻两次识别一致的前缀才提交为稳定文本。
- 中文翻译以稳定短语为单位触发：出现标点、累计 4–8 个新词，或等待约 600ms 时翻译一次。
- 这是二期首选方案：改动可控，并且能保留中文检测和一期回退能力。

### 4.3 实验档：极低延迟流式 ASR

- 评估 sherpa-onnx 的英文流式 Transducer/Zipformer/Parakeet 路线。
- 流式模型原生按小块解码，首字延迟可以显著低于“反复跑 Whisper 窗口”。
- sherpa-onnx 官方项目支持 Windows、C#、流式 ASR、VAD 和说话人相关功能，适合减少 Python/HTTP 层。
- 风险：英语口音、游戏术语、多人重叠语音的实际准确率必须重新做 A/B 测试；英文专用模型也不能直接承担 Discord 中文绕过逻辑。
- 只有推荐档达不到目标时才替换默认引擎。

## 5. 具体性能策略

### 5.1 识别侧

1. 本地模型服务启动时预热，不让第一次 Discord 语音承担模型初始化。
2. 音频持续送入同一个会话，不再等到整句结束后才发 HTTP 请求。
3. 用 WebSocket、命名管道或持续 TCP 会话传输 40ms PCM，避免每段新建请求和复制大字节数组。
4. ASR、翻译和 UI 使用三个独立工作队列；ASR 临时版本采用“只保留最新”，最终结果永不丢弃。
5. 保存词级时间戳和已确认前缀，只重算尚未稳定的尾部。
6. 游戏启动或 GPU 占用过高时，可以自动从 GPU 低延迟档切回 CPU 稳定档，但切换只能发生在语音段之间。

### 5.2 翻译侧

1. OPUS-MT 模型常驻内存，保持 CTranslate2 INT8。
2. 不翻译不稳定尾部；稳定短语到达阈值后再增量翻译。
3. 保留上一段英文上下文，处理代词和短句；发送给模型的上下文不重复显示。
4. 常用游戏术语表在翻译前统一，例如 push、rotate、knocked、revive、third floor。
5. 最终定稿时允许做一次整句校正，只替换当前字幕行，不新增重复历史。

### 5.3 UI 与背压

1. 当前英文和中文各只有一条可变行；定稿后才进入历史。
2. 识别暂时追不上时显示轻量状态，不阻塞音频采集线程。
3. 队列积压时丢弃过期的“临时版本”，绝不丢弃原始音频或最终句段。
4. 给设置界面增加“稳定 / 游戏低延迟 / 实验流式”三档，以及实时显示 ASR、翻译、总延迟和显存占用。

## 6. 我方中文发送如何提速

一期“按住右 Alt，说完并松开，再翻译和播放”应继续作为默认安全模式，因为已发送的语音无法撤回。

二期可以增加可选的“分句抢跑”：

1. 按住右 Alt 后立即流式识别中文，并在悬浮窗显示临时中文。
2. 检测到 400–700ms 的短停顿或明确标点后，确认一个中文短句。
3. 立即翻译该短句并合成第一段英文，后面的中文仍继续识别。
4. TTS 分段进入同一个播放队列，段间增加很短的自然停顿。
5. 已经播放的英文不能回改，因此只允许稳定短句进入 TTS 队列。

这种模式能让对方更早听到第一段英文，但会牺牲部分整句上下文。建议作为可选开关，不替换一期默认模式。

## 7. 防回声与并发原则

- TTS 写入 VB-CABLE 时继续暂停 Discord 入站识别接受状态，保留现有排空保护。
- 流式 ASR 内部可以持续运行，但回声隔离期间的 PCM 必须直接丢弃，不能晚些补送。
- 本人原声直通、中文采集、英文 TTS 三种状态仍然互斥。
- 多个翻译结果可以排队，但同一时间只允许一个英文 TTS 播放器写入虚拟声卡。

## 8. 多人说话的边界

Discord Process Loopback 是混合音轨。纯本地说话人聚类只能标为“说话人 1/2”，不能直接获得 Discord 用户名；两个人同时说话时还会明显降低识别和聚类质量。真正显示用户名需要 Discord Bot 分用户接收音轨，这是独立架构，不应和低延迟改造捆在第一步。

## 9. 推荐实施顺序

### 二期 A：先做增量字幕

- 定义 partial / stable / final 消息协议。
- 当前字幕行支持原地替换。
- 增加延迟埋点和可重复的长音频回放测试。
- 保持 CPU 一期模式可一键回退。

### 二期 B：加入 GPU 推荐档

- 安装匹配的 CUDA 运行环境。
- faster-whisper small 改为 CUDA `int8_float16`，OPUS-MT 保持 CPU。
- 在 Discord 实际频道和 3A 游戏同时运行时测 P50/P95、显存与帧时间。
- 若显存或帧时间超过阈值，自动提示切回稳定档，不静默牺牲游戏性能。

### 二期 C：增量翻译与中文分句抢跑

- 稳定英文短语增量英译中。
- 最终整句校正。
- 中文稳定短句翻译、TTS 队列和不可撤回保护。

### 二期 D：实验引擎

- 用同一批 Discord 录音离线 A/B 测试 sherpa-onnx 流式模型。
- 只有其准确率和游戏术语表现达到门槛，才提供为可选引擎。
- 说话人聚类和 Discord Bot 分轨另立项目，不阻塞实时字幕交付。

## 10. 验收门槛

- 连续 10 分钟 Discord 英语，无崩溃、无音频队列无限增长。
- 30 秒长句句首和句尾完整，最终字幕不重复。
- P50 首个英文临时字幕不高于 1.2 秒，P95 不高于 2 秒。
- P50 首段中文不高于 3 秒，P95 不高于 5 秒。
- 最终结果平均回改不超过 2 次，已确认历史不回改。
- 同时运行目标 3A 游戏时，主观无明显卡顿；用帧时间数据确认，而不是只看平均 FPS。
- GPU 推荐档失败时可无损回退到一期 CPU 稳定档。

## 11. 推荐结论

二期首先实现“faster-whisper small GPU 增量识别 + LocalAgreement-2 + OPUS-MT CPU 增量翻译”，不要直接上大型端到端语音翻译模型。它最符合当前 Windows、Discord、4070 Ti 和 3A 游戏并行运行的约束，也最容易保留一期已经验证的完整性、中文绕过和防回声能力。

参考资料：

- [faster-whisper 官方项目](https://github.com/SYSTRAN/faster-whisper)
- [Whisper-Streaming 官方项目与 LocalAgreement](https://github.com/ufal/whisper_streaming)
- [SimulStreaming 官方项目](https://github.com/ufal/SimulStreaming)
- [sherpa-onnx 官方项目](https://github.com/k2-fsa/sherpa-onnx)
- [CTranslate2 量化说明](https://opennmt.net/CTranslate2/quantization.html)
- [CTranslate2 性能建议](https://opennmt.net/CTranslate2/performance.html)
