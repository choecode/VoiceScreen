# VoiceScreen 实时化路线与实施状态

更新日期：**2026-08-25**。

这份文件不再描述尚未开始的“二期方案”，而是记录已经落地的能力、当前性能基线和下一阶段优先级。生产架构见 [PROJECT_GUIDE.md](PROJECT_GUIDE.md)，部署步骤见 [DEPLOYMENT.md](DEPLOYMENT.md)。

## 1. 已完成

### 接收字幕

- 选定任意桌面应用及其子进程树，而不是只写死 Discord；
- Process Loopback 捕获会话自动恢复；
- Silero VAD 替代 RMS 作为主语音判据；
- Qwen3-ASR-1.7B 服务器端流式会话；
- 临时字幕原地更新，最终定稿才写入历史；
- ASR 与翻译独立队列，过期临时版本可丢弃；
- Qwen3-4B 语义边界判断，固定时长只保留为上限；
- 周期幻觉、异常膨胀和重复翻译过滤；
- 用户手动滚动浏览历史时暂停自动跟随，回到底部后恢复。

### 中文发送

- 右 Alt 半双工录音、翻译和英文发送；
- 可选自然短句抢跑，不必一直等到松键；
- 抢跑内容与最终文本按内容对齐，避免重复发送；
- Qwen3-TTS 私人音色；
- 长英文自然分块，第一块先播、后续块边播边生成；
- 避免在冠词、介词、连词和 `don't` 等悬空词后切分；
- TTS、耳机监听和 VB-CABLE 完整排空后再恢复原声。

### 可用性

- 缺少 VB-CABLE 时降级为仅字幕，不阻断接收链路；
- Spark 音色失败时回退 Windows Speech；
- Silero 失败时回退 RMS；
- 固定 14 号字幕，主界面简化；
- Windows 本机 Whisper/Sherpa/OPUS 保留为离线回退；
- 生产 Compose 与 CosyVoice 实验 profile 隔离。

## 2. 当前延迟基线

延迟必须分阶段看，不能只报告一个“总延迟”：

```text
语音开始
  → VAD 确认
  → ASR 临时结果
  → ASR 最终结果
  → 翻译完成
  → 第一段 TTS 可播放
  → 全部 TTS 播放完成
```

28 号 Spark 预热后的固定中文样本：

| 中文长度 | 翻译到首个 Qwen TTS 分块可播放 |
|---:|---:|
| 31 字 | 约 5.6 秒 |
| 67 字 | 约 4.3 秒 |
| 124 字 | 约 8.0 秒 |

124 字中文的翻译本身约 4.3 秒。未分块 Qwen 整句路径约 22.8 秒才能得到完整 WAV，已经退出生产播放路径。

详细 A/B 和 VAD 数据见 [TEST_REPORT.md](TEST_REPORT.md)。

## 3. 已评估但未选为默认

### CosyVoice 3

优点：支持真实流式 PCM，整句场景的首音频明显早于未分块 Qwen。

未选原因：在相同 Spark、相同译文和私人参考音色下，客户端自然分块后的 Qwen 首音频三组样本都更快，总 RTF 也更好；CosyVoice 还增加约 9 GB 模型和更多 ARM64 兼容层。

状态：保留在 `experiments` profile，便于未来新版本复测，生产停止。

### SeamlessStreaming / 端到端 S2ST

潜在优点：理论上可以减少 ASR、翻译、TTS 三段等待和中间文本误差。

当前不迁移的原因：现有产品需要可见原文、可编辑/可回溯字幕、私人指定音色、Windows 进程捕获和本地回退；端到端模型会降低模块可观测性，也需要重新验证 ARM64、显存、语言质量和许可证。

### LiveKit / WebRTC Agent 管线

LiveKit 适合浏览器会议、远程房间和多用户服务。VoiceScreen 当前音频源与输出都在同一台 Windows 电脑，直接 Process Loopback + WASAPI 更短、更容易诊断。若未来做跨设备或多人 SaaS，再重新评估 WebRTC。

### whisper.cpp / whisper_streaming

其滑动窗口、稳定前缀和 LocalAgreement 思想已经被吸收。当前生产 ASR 已切到 Spark Qwen，本机 Whisper 仍是回退，因此没有必要同时维护另一套 C++ 运行时。

## 4. 下一阶段优先级

### P0：真实通话质量度量

- 为每个最终语句记录匿名阶段耗时：VAD、ASR 首字、ASR final、翻译、TTS 首块；
- 增加队列深度、丢弃临时快照数和服务重试次数；
- 在 UI 中显示当前语句延迟，而不是只写日志；
- 建立 30–60 分钟 Discord/YouTube 长时间压力回放，检查延迟是否累积。

验收：运行一小时后，P95 延迟不随时间单调增长，队列能回落到零。

### P0：输入与边界语料回归

- 收集经过授权的真实英语口音、游戏语音、背景音乐和多人重叠样本；
- 为每个样本记录期望句界、不可丢失的关键词、否定词、数字和方位；
- 自动对比断句丢字、重复率、最终文本改写距离和翻译覆盖率。

验收：每次修改 VAD、流式回滚或边界阈值，都能用同一套音频回归。

### P1：发送首音频继续优化

- 让翻译模型按可提交语义块流式输出，而不是必须得到整段译文；
- 对第一个英文块设置更小的目标长度，后续块保持自然韵律；
- 在不改变音色的前提下评估 Qwen3-TTS 新版真正流式接口；
- 增加 TTS 失败块重试和请求取消后的显存清理观测。

验收：中等中文的第一段英文 P50 小于 3 秒、P95 小于 5 秒，同时不提高语义错切率。

### P1：弱网和 Spark 故障体验

- 健康检查缓存与明确的“服务不可达 / 令牌错误 / 模型加载中”状态；
- 语句边界内不切模型，下一语句才回退本机；
- Spark 恢复后允许无损重新连接，而不是必须重启整个应用。

验收：拔网线、重启容器、令牌错误三种故障都有不同提示，字幕或发送能按设计回退。

### P2：多人和说话人

- Discord Process Loopback 是混合音轨，无法直接得到真实用户名；
- 可研究本地说话人聚类，只显示“说话人 1/2”，不得假装知道 Discord 用户名；
- 真正分用户需要 Discord Bot 或 WebRTC 分轨，属于新产品边界。

## 5. 长期验收指标

| 指标 | 目标 |
|---|---:|
| ASR 临时首字 P50 | ≤ 1.5 秒 |
| 英译中临时结果 P50 | ≤ 2.5 秒 |
| 停顿后最终定稿 P50 | ≤ 1.5 秒 |
| 中文发送首段英文 P50 | ≤ 3.0 秒 |
| 一小时运行后的队列残留 | 0 |
| 音乐/纯音假语音率 | 接近 0 |
| 最终历史重复率 | < 1% |
| 句首/句尾关键字丢失 | 0 个已知回归样本 |

这些是工程目标，不是已经达到的 SLA。正式声明性能时必须同时报告硬件、模型版本、预热状态、网络、语料长度、P50/P95 和测试次数。
