# VoiceScreen：Discord 纯本地双向语音翻译方案

> **历史设计文档。** 本文记录早期“Windows 本机 Whisper + OPUS-MT + Windows Speech”方案，保留用于理解演进过程，不代表当前默认配置。2026-08-25 的生产架构已经升级为 Windows 客户端 + DGX Spark/Qwen，并支持任意应用进程监听、Silero VAD、语义断句、私人音色和长句分块。当前说明请以 [README](README.md)、[项目说明](PROJECT_GUIDE.md) 和 [部署指南](DEPLOYMENT.md) 为准。

## 1. 最终需求

VoiceScreen 面向 Windows 11 与 Discord 语音通话，核心目标是：

- 对方说英语时，只监听 Discord 的声音，在左上角半透明悬浮窗中逐句显示英文原文和中文翻译。
- 自己平时说话时，中文原声正常传给 Discord。
- 按住右 Alt 说中文时，临时停止原声发送；松开后在本机完成中文识别、中译英和英文语音合成，再把英文语音发给 Discord。
- 悬浮窗同步显示自己说的中文和最终发送的英文。
- 实际发送的英文可同步在实体耳机试听，并提供只在耳机播放的中文文本翻译测试按钮。
- 英文音色从 Windows 已安装语音中选择，支持男声、女声；实际发送英文的耳机复读可单独关闭。
- 处理延迟目标为 5 秒以内，并尽量不占用正在运行 3A 游戏的显卡。
- 所有识别、翻译和语音合成都在本机完成，不调用讯飞或其他外部 API。

## 2. 总体架构

### 接收方向：外国人说英语

```text
Discord 进程声音
  → Windows Process Loopback（仅 Discord 进程树）
  → 本地能量 VAD 分句
  → faster-whisper small（英文识别）
  → OPUS-MT en-zh CPU INT8（仅检测为英文时英译中）
  → 悬浮窗：EN 原文 + 中 文译文
```

Process Loopback 采用包含目标进程树的方式捕获 Discord 及其 Electron 子进程。捕获初始化失败时直接报错，不降级为“监听整台电脑”，因此游戏中的中文配音不会进入字幕链路。

### 发送方向：自己说中文

```text
默认状态：实体麦克风 → VoiceScreen → VB-CABLE → Discord

按住右 Alt：
实体麦克风 → 临时录音，停止原声直通
  → 松开按键
  → faster-whisper small（中文识别）
  → OPUS-MT zh-en CPU INT8（中译英）
  → Windows Speech（离线英文 TTS）
  → VB-CABLE → Discord
```

英文 TTS 完成并排空播放缓冲区后，程序恢复中文原声直通。

实际发送时，同一份英文 PCM 会分别写入 VB-CABLE 和用户选择的实体耳机。测试按钮则只写入实体耳机，不进入 VB-CABLE，因此不会打扰 Discord 通话。

## 3. 为什么不会发生翻译死循环

程序使用明确的半双工状态机：

1. `Listening`：监听 Discord 英语，同时转发真实麦克风。
2. `CapturingChinese`：按住右 Alt 后录制中文，暂停原声转发。
3. `Translating`：本地识别和翻译，仍不播放合成音。
4. `SpeakingEnglish`：向 VB-CABLE 播放英文，同时暂停 Discord 接收识别。
5. 播放结束并等待缓冲排空后回到 `Listening`。

另外，接收链路来自 Discord 进程输出，发送链路进入 Discord 的虚拟麦克风，两者不是同一个音频端点。状态门控与物理通道隔离共同避免“识别自己刚合成的英文并再次翻译”的循环。

耳机试听也不会导致循环：接收端只捕获 Discord 进程而非整张声卡；播放期间 `ShouldAcceptRemoteResult` 为假；实体麦克风直通保持静音，直到耳机和虚拟声卡缓冲都排空。

## 4. 本地模型选择

| 环节 | 模型或组件 | 选择原因 |
|---|---|---|
| 语音识别 | faster-whisper small / CPU INT8 | 中英文均可用，准确率与 CPU 延迟较平衡 |
| 文本翻译 | Helsinki-NLP OPUS-MT 双向专用模型 / CTranslate2 INT8 | 专用机器翻译，不会像聊天模型一样替玩家回答 |
| 英文发音 | Windows Speech | 离线、无需额外密钥，接入简单 |
| 分句 | 本地能量 VAD | 无网络依赖，实时开销小 |

模型首次下载与转换需要联网；完成后 Whisper 与 OPUS-MT 均以离线模式加载，只通过本机回环服务调用。

## 5. 音频设备配置

程序内：

- 实体麦克风：HyperX 麦克风。
- 虚拟麦克风播放端：`CABLE Input (VB-Audio Virtual Cable)`。
- Discord 接收：自动跟踪 Discord 进程，不选择耳机混音。

Discord 内：

- 麦克风：`CABLE Output (VB-Audio Virtual Cable)`。
- 扬声器：HyperX 实体耳机。

不能把 Discord 扬声器设成 CABLE，否则声音会被送回输入链路，容易产生回声；也不能让 Discord 直接选 HyperX 麦克风，否则它收不到程序合成的英文。

## 6. 悬浮窗设计

- 无边框、半透明、置顶、鼠标穿透，默认放在屏幕左上角。
- 每句独立换行，同时显示英文原文和中文译文。
- 长文本按窗口宽度自动换行，不使用横向滚动条。
- 保留最近若干句，使玩家能看到上下文；新句自动滚到可见位置。
- 自己的字幕使用不同颜色显示“我说”和“已发送”，便于确认机器最终发送了哪些英文词。

## 7. 性能策略

- Whisper 与 OPUS-MT 固定使用 CPU，避免与 3A 游戏争抢显卡显存。
- 模型在启动时预热，降低第一次按键后的等待时间。
- 接收和发送共用推理锁，防止两个 CPU 模型任务同时运行造成卡顿。
- VAD 只把成句音频送入 Whisper，不对静音持续推理。
- 字幕队列和音频队列均有上限，忙时丢弃最旧任务而不是无限积压。

## 8. 隐私与安全

- 运行时不需要 APPID、APIKey 或 Secret。
- 不保存原始音频和字幕历史。
- 不注入 Discord 或游戏，不读取进程内存。
- 只使用 Windows 公共音频接口和本机回环服务。
- 历史讯飞代码、配置界面和云端自测项目已从仓库删除。

## 9. 验收标准

1. 游戏发声不产生字幕，Discord 对方英语能够产生双语字幕。
2. 普通中文原声可被对方听到，且没有明显回声。
3. 按住右 Alt 说中文、松开后，对方听到完整英文，悬浮窗显示对应中英文。
4. 合成语音不会再次触发接收翻译。
5. 长句正确换行，连续多句仍保持可读。
6. 模型准备完成后断开互联网，以上流程仍能工作。
7. 预热后的常见短句在 5 秒内完成处理。
